export const selectPostInfo = state => state.comments
export const selectComments = state => state.comments.comments;

/* export const selectPostById = id => state =>
  state.posts.posts.find(post => post.id === id); */